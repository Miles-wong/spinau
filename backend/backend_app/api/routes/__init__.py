"""HTTP route modules for the backend API.

Each module defines a Flask Blueprint for a functional area (tickets,
conversational intake, form submission, attachments). The application factory
imports these modules and registers their blueprints on startup.
"""
