"""Shared backend constants package.

This package stores stable, reusable definitions (for example incident schema
metadata) that must remain consistent across extraction, question flow, and
route validation logic.
"""
